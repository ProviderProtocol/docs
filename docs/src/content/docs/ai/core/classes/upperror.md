---
title: "Class: UPPError"
---

[**@providerprotocol/ai**](../../../index.md)

***

[@providerprotocol/ai](./index.md) / [@providerprotocol/ai](../index.md) / UPPError

# Class: UPPError

Defined in: [src/types/errors.ts:111](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L111)

Unified Provider Protocol Error.

All provider-specific errors are normalized to this type, providing
a consistent interface for error handling across different AI providers.

## Examples

```typescript
throw new UPPError(
  'API key is invalid',
  'AUTHENTICATION_FAILED',
  'openai',
  'llm',
  401
);
```

```typescript
// Wrapping a provider error
try {
  await openai.chat.completions.create({ ... });
} catch (err) {
  throw new UPPError(
    'OpenAI request failed',
    'PROVIDER_ERROR',
    'openai',
    'llm',
    err.status,
    err
  );
}
```

## Extends

- `Error`

## Constructors

### Constructor

> **new UPPError**(`message`, `code`, `provider`, `modality`, `statusCode?`, `cause?`): `UPPError`

Defined in: [src/types/errors.ts:140](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L140)

Creates a new UPPError instance.

#### Parameters

##### message

`string`

Human-readable error description

##### code

[`ErrorCode`](../type-aliases/errorcode.md)

Normalized error code for programmatic handling

##### provider

`string`

Name of the provider that generated the error

##### modality

[`Modality`](../type-aliases/modality.md)

The modality that was being used

##### statusCode?

`number`

HTTP status code from the provider's response

##### cause?

`Error`

The original error being wrapped

#### Returns

`UPPError`

#### Overrides

`Error.constructor`

## Properties

### cause?

> `readonly` `optional` **cause**: `Error`

Defined in: [src/types/errors.ts:125](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L125)

The original error that caused this UPPError, if wrapping another error

#### Overrides

`Error.cause`

***

### code

> `readonly` **code**: [`ErrorCode`](../type-aliases/errorcode.md)

Defined in: [src/types/errors.ts:113](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L113)

Normalized error code for programmatic handling

***

### message

> **message**: `string`

Defined in: node\_modules/typescript/lib/lib.es5.d.ts:1077

#### Inherited from

`Error.message`

***

### modality

> `readonly` **modality**: [`Modality`](../type-aliases/modality.md)

Defined in: [src/types/errors.ts:119](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L119)

The modality that was being used when the error occurred

***

### name

> `readonly` **name**: `"UPPError"` = `'UPPError'`

Defined in: [src/types/errors.ts:128](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L128)

Error class name, always 'UPPError'

#### Overrides

`Error.name`

***

### provider

> `readonly` **provider**: `string`

Defined in: [src/types/errors.ts:116](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L116)

Name of the provider that generated the error

***

### stack?

> `optional` **stack**: `string`

Defined in: node\_modules/typescript/lib/lib.es5.d.ts:1078

#### Inherited from

`Error.stack`

***

### statusCode?

> `readonly` `optional` **statusCode**: `number`

Defined in: [src/types/errors.ts:122](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L122)

HTTP status code from the provider's response, if available

***

### stackTraceLimit

> `static` **stackTraceLimit**: `number`

Defined in: node\_modules/@types/node/globals.d.ts:67

The `Error.stackTraceLimit` property specifies the number of stack frames
collected by a stack trace (whether generated by `new Error().stack` or
`Error.captureStackTrace(obj)`).

The default value is `10` but may be set to any valid JavaScript number. Changes
will affect any stack trace captured _after_ the value has been changed.

If set to a non-number value, or set to a negative number, stack traces will
not capture any frames.

#### Inherited from

`Error.stackTraceLimit`

## Methods

### toJSON()

> **toJSON**(): `Record`\<`string`, `unknown`\>

Defined in: [src/types/errors.ts:180](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L180)

Converts the error to a JSON-serializable object.

#### Returns

`Record`\<`string`, `unknown`\>

Plain object representation suitable for logging or transmission

***

### toString()

> **toString**(): `string`

Defined in: [src/types/errors.ts:165](https://github.com/ProviderProtocol/ai/blob/c1fa49d2a211d9e50c9b51e4dc1cbd9d945b925a/src/types/errors.ts#L165)

Creates a string representation of the error.

#### Returns

`string`

Formatted error string including code, message, provider, and modality

***

### captureStackTrace()

#### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Defined in: node\_modules/@types/node/globals.d.ts:51

Creates a `.stack` property on `targetObject`, which when accessed returns
a string representing the location in the code at which
`Error.captureStackTrace()` was called.

```js
const myObject = {};
Error.captureStackTrace(myObject);
myObject.stack;  // Similar to `new Error().stack`
```

The first line of the trace will be prefixed with
`${myObject.name}: ${myObject.message}`.

The optional `constructorOpt` argument accepts a function. If given, all frames
above `constructorOpt`, including `constructorOpt`, will be omitted from the
generated stack trace.

The `constructorOpt` argument is useful for hiding implementation
details of error generation from the user. For instance:

```js
function a() {
  b();
}

function b() {
  c();
}

function c() {
  // Create an error without stack trace to avoid calculating the stack trace twice.
  const { stackTraceLimit } = Error;
  Error.stackTraceLimit = 0;
  const error = new Error();
  Error.stackTraceLimit = stackTraceLimit;

  // Capture the stack trace above function b
  Error.captureStackTrace(error, b); // Neither function c, nor b is included in the stack trace
  throw error;
}

a();
```

##### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

##### Returns

`void`

##### Inherited from

`Error.captureStackTrace`

#### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Defined in: node\_modules/bun-types/globals.d.ts:1042

Create .stack property on a target object

##### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

##### Returns

`void`

##### Inherited from

`Error.captureStackTrace`

***

### isError()

#### Call Signature

> `static` **isError**(`error`): `error is Error`

Defined in: node\_modules/typescript/lib/lib.esnext.error.d.ts:23

Indicates whether the argument provided is a built-in Error instance or not.

##### Parameters

###### error

`unknown`

##### Returns

`error is Error`

##### Inherited from

`Error.isError`

#### Call Signature

> `static` **isError**(`value`): `value is Error`

Defined in: node\_modules/bun-types/globals.d.ts:1037

Check if a value is an instance of Error

##### Parameters

###### value

`unknown`

The value to check

##### Returns

`value is Error`

True if the value is an instance of Error, false otherwise

##### Inherited from

`Error.isError`

***

### prepareStackTrace()

> `static` **prepareStackTrace**(`err`, `stackTraces`): `any`

Defined in: node\_modules/@types/node/globals.d.ts:55

#### Parameters

##### err

`Error`

##### stackTraces

`CallSite`[]

#### Returns

`any`

#### See

https://v8.dev/docs/stack-trace-api#customizing-stack-traces

#### Inherited from

`Error.prepareStackTrace`
