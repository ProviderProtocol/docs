---
title: Provider Protocol
description: 0-Dependency AI Developer first SDK
template: splash
hero:
  tagline: Built by developers with AI First application developmental experience to simplify and streamline the next generation of AI First software. 
  image:
    file: ../../assets/logo.png
    alt: Provider Protocol Logo
  actions:
    - text: Get Started
      link: /introduction/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/ProviderProtocol/ai
      icon: external
---

import { Badge, Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid>
	<Card title="Multi-Provider" icon="rocket">
		One interface, any provider, any surface. Your backend code _is_ your frontend code - your OpenAI code _is_ your anthropic code. 
	</Card>
	<Card title="Type-Safe" icon="approve-check">
		Full TypeScript support with intelligent autocompletion and compile-time checks.
	</Card>
	<Card title="Transparent" icon="open-book">
		Build without worrying about magic, read provider official documentation and you _already_ know how to use Provider Protocol. 
	</Card>
	<Card title="Modular" icon="puzzle">
		Purpose-built interfaces for `llm()`, `embedding()`, and `image()`.
	</Card>
</CardGrid>

## Quick Start

```typescript
import { llm, embedding } from '@providerprotocol/ai';
import { anthropic } from '@providerprotocol/ai/anthropic';
import { openai } from '@providerprotocol/ai/openai';

// LLM inference with Claude
const claude = llm({
  model: anthropic('claude-sonnet-4-20250514'),
  system: 'You are a helpful assistant.',
});

const response = await claude.generate('Explain quantum computing');
console.log(response.text);

// Generate embeddings with OpenAI
const embedder = embedding({
  model: openai('text-embedding-3-large'),
});

const vector = await embedder.embed('Hello, world!');
```

## Specification

<Badge text="UPP-1.3" variant="note" /> <Badge text="Current" variant="success" />

| Interface | Status | Description |
|-----------|--------|-------------|
| **LLM** | <Badge text="Stable" variant="success" size="small" /> | Text generation, streaming, tools, structured output |
| **Embedding** | <Badge text="Stable" variant="success" size="small" /> | Vector embeddings, batch processing, similarity |
| **Image** | <Badge text="Stable" variant="success" size="small" /> | Generation, editing, variations |
| Audio | <Badge text="Planned" variant="default" size="small" /> | Speech-to-text, text-to-speech |

---

<CardGrid>
	<Card title="Introduction" icon="document">
		Learn about [Provider Protocol's goals and architecture](/introduction/).
	</Card>
	<Card title="Quick Start" icon="laptop">
		Follow the [Quick Start guide](/guide/quick-start/) to begin building.
	</Card>
</CardGrid>
